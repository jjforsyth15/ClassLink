# ClassLink

Monorepo:
- `frontend/` React + Vite + TypeScript
- `backend/` FastAPI

## Run backend
cd backend
# activate venv
uvicorn main:app --reload --port 8000

## Run frontend
cd frontend
npm install
npm run dev
